import {Component, EventEmitter, Input, OnInit, Output, TemplateRef, ViewChild} from "@angular/core";

@Component({
    standalone: false,
    templateUrl: './templates/org-global.html'
})
export class OrgGlobalLayout { onActivate(e){} }