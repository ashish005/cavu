import {Component, EventEmitter, Input, OnInit, Output, TemplateRef, ViewChild} from "@angular/core";

@Component({
    standalone: false,
    templateUrl: './templates/org.html'
})
export class OrgLayout { onActivate(e){} }